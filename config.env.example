# Face Label Migration Tool - Configuration
# Copy this file to config.env and update the values for your environment.

# =============================================================================
# MS Photos Legacy Database
# =============================================================================
# Path to the MS Photos Legacy SQLite database (MediaDb.v1.sqlite)
# You can find this in: %LOCALAPPDATA%\Packages\Microsoft.PhotosLegacy_8wekyb3d8bbwe\LocalState\
MS_PHOTOS_DB=path/to/MediaDb.v1.sqlite

# =============================================================================
# Immich API Connection
# =============================================================================
# Your Immich server URL
IMMICH_API_URL=http://localhost:2283

# API key from Immich (Settings > API Keys)
IMMICH_API_KEY=your-api-key-here

# =============================================================================
# Immich PostgreSQL Database
# =============================================================================
# Direct database access is required for reading face position data
IMMICH_DB_HOST=localhost
IMMICH_DB_PORT=5432
IMMICH_DB_NAME=immich
IMMICH_DB_USER=postgres
IMMICH_DB_PASSWORD=your-db-password

# =============================================================================
# Migration Settings
# =============================================================================
# Minimum IoU (Intersection over Union) score for face matching (0.0 - 1.0)
MIN_OVERLAP_SCORE=0.3

# Minimum number of photos in a cluster to consider for matching
MIN_PHOTOS_IN_CLUSTER=1

# How to resolve conflicts when multiple matches exist
# Options: highest_score, most_photos, manual
CONFLICT_RESOLUTION=highest_score

# =============================================================================
# Path Mappings (Optional)
# =============================================================================
# JSON object mapping Immich container paths to local filesystem paths.
# This is used for thumbnail generation when Immich uses external libraries.
# Example:
# PATH_MAPPINGS='{"/external/photos": "C:/Users/you/Pictures", "/external/backup": "D:/Backup"}'
PATH_MAPPINGS={}
